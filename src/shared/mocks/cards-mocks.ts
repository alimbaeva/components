const CardMock = {
  image: {
    src: '/images/moun-1.avif',
    alt: 'Озеленение офиса',
  },
  title: 'Озеленение офиса',
  description:
    'Живые и искусственные растения для рабочих и клиентских зон. Эстетика, комфорт и улучшение микроклимата.',
  description_long: `Подавляющее большинство растений — фотоавтотрофные организмы, использующие световую энергию для синтеза органики (глюкозы) из неорганических соединений (углекислый газ и вода). Некоторые представители перешли к вторичному гетеротрофному способу питания (облигатный паразитизм, микогетеротрофность). Например, гетеротрофом является бесхлорофилльное растение петров крест, паразитирующий на корнях деревьев и кустарников и получающий готовые питательные вещества из организма-хозяина. Процессы роста и развития растения неразрывно связаны между собой: рост является частью индивидуального развития. Однако в одном и том же организме процессы роста и развития могут сочетаться различным образом. Растение может находиться в состоянии активного роста, но вместе с тем медленно развиваться или, наоборот, оно может быстро развиваться при замедленном росте. Показателем темпов развития, как правило, служит переход растений к репродукции. Активность ростовых процессов оценивают по скорости увеличения массы, объёма, размеров растения. Например, у однолетних растений с момента их зацветания наблюдается частичная и даже полная приостановка процессов роста побега`,
  price: '40000',
  companiesCount: 142,
  tags: [
    'Вертикальное озеленение',
    'Абонентское обслуживание',
    'Гидропонные системы',
    'Обслуживание под ключ',
  ],
  logos: [
    {
      id: '1',
      src: '/images/cards/hors_192.png',
      alt: 'AG',
    },
    {
      id: '2',
      src: '/images/cards/net_192.png',
      alt: 'ЗЕЛЕНЫЙ',
    },
    {
      id: '3',
      src: '/images/cards/smile_192.png',
      alt: 'EverGreen',
    },
  ],
}

const ProjectCardMock = {
  labels: ['озеленение', 'Гидропонные', 'Вертикальное', 'Живые'],
  image: {
    src: '/images/moun-1.avif',
    alt: 'Озеленение офиса',
  },
  logo: {
    id: '1',
    src: '/images/cards/hors_192.png',
    alt: 'AG',
  },
  price: '40000',
  title: 'Озеленение офиса',
  devTitle: 'Озеленение офиса',
  description:
    'Живые и искусственные растения для рабочих и клиентских зон. Эстетика, комфорт и улучшение микроклимата.',
  description_long: `Подавляющее большинство растений — фотоавтотрофные организмы, использующие световую энергию для синтеза органики (глюкозы) из неорганических соединений (углекислый газ и вода). Некоторые представители перешли к вторичному гетеротрофному способу питания (облигатный паразитизм, микогетеротрофность). Например, гетеротрофом является бесхлорофилльное растение петров крест, паразитирующий на корнях деревьев и кустарников и получающий готовые питательные вещества из организма-хозяина. Процессы роста и развития растения неразрывно связаны между собой: рост является частью индивидуального развития. Однако в одном и том же организме процессы роста и развития могут сочетаться различным образом. Растение может находиться в состоянии активного роста, но вместе с тем медленно развиваться или, наоборот, оно может быстро развиваться при замедленном росте. Показателем темпов развития, как правило, служит переход растений к репродукции. Активность ростовых процессов оценивают по скорости увеличения массы, объёма, размеров растения. Например, у однолетних растений с момента их зацветания наблюдается частичная и даже полная приостановка процессов роста побега`,
}

const serviceCardMock = {
  title: 'Карточка услуг',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/cards/service-card/service-card.tsx',
  descriptionContent: `
    <h3>Архитектура карточки предложения</h3>
    <p>
      Этот компонент предназначен для отображения конкретного оффера в агрегаторах. Он визуально разделен на медиа-зону, блок с деталями (цена, описание) и футер с социальным доказательством (логотипы партнеров).
    </p>

    <h3>1. Технические особенности реализации</h3>
    <ul>
        <li><strong>Контейнер Article:</strong> Использование семантического тега <code>article</code> вместо <code>div</code> улучшает SEO и доступность, так как карточка является самостоятельным смысловым блоком.</li>
        <li><strong>Контроль изображений:</strong> Комбинация <code>relative</code> на родителе и <code>fill</code> с <code>object-cover</code> на компоненте Next.js Image позволяет гибко управлять пропорциями картинки без искажений.</li>
        <li><strong>Группировка логотипов:</strong> Секция с логотипами реализована через Flexbox, что позволяет легко масштабировать количество партнеров.</li>
    </ul>

    <h3>2. На что обратить внимание в Tailwind CSS (Checklist)</h3>
    <ul>
        <li><strong>Позиционирование:</strong> Для корректной работы <code>fill</code> у картинки родительскому блоку <strong>обязательно</strong> нужен класс <code>relative</code>.</li>
        <li><strong>Скругление углов:</strong> При использовании <code>rounded-2xl</code> на основном контейнере не забывайте про <code>overflow-hidden</code>, иначе дочерние элементы (например, картинка сверху) перекроют скругления.</li>
        <li><strong>Гибкость футера:</strong> Классы <code>flex items-center justify-between</code> — это стандарт для разделения контента по разным краям карточки.</li>
    </ul>

    <h3>3. Рекомендации по UX и UI</h3>
    <ul>
        <li><strong>Ограничение текста:</strong> Если описание (description) может быть разной длины, используйте плагин Line Clamp: класс <code>line-clamp-2</code> сохранит единую высоту карточек в сетке.</li>
        <li><strong>Интерактивность:</strong> Добавьте <code>transition-all hover:shadow-2xl</code> на весь контейнер. Это даст пользователю мгновенный визуальный отклик при наведении.</li>
        <li><strong>Доступность (A11y):</strong> Для кнопок без уникального текста (если их много на странице) желательно добавлять <code>aria-label</code> с названием конкретного предложения.</li>
    </ul>

    <h3>📋 Шпаргалка по оптимизации</h3>
    <p>
    1. <strong>Image Alt:</strong> Всегда делайте фоллбек для alt-текста: <code>alt={image.alt ?? 'предложение'}</code>.<br/>
    2. <strong>Gap вместо Margin:</strong> Используйте <code>gap-2</code> в flex-контейнерах вместо задания отступов каждому дочернему элементу.<br/>
    3. <strong>Размеры (Width):</strong> Класс <code>max-w-[340px]</code> (или <code>max-w-85</code>) предотвращает чрезмерное растягивание карточки на десктопных мониторах.
    </p>
  `,
}

const serviceSnippetCardMock = {
  title: 'Карточка услуг (snippet Card)',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/cards/service-snippet/service-snippet.tsx',
  descriptionContent: `
    <h3>1. Технические особенности реализации</h3>
    <ul>
        <li><strong>Механика перекрытия:</strong> Применение невидимого компонента <code>Link</code> с классами <code>absolute inset-0 size-full</code> создает единую интерактивную область на всю площадь карточки, упрощая взаимодействие (UX).</li>
        <li><strong>Приоритет взаимодействия:</strong> Для корректной работы внутренних кнопок и тегов поверх основной ссылки используется класс <code>relative z-10</code>. Это поднимает элементы по оси Z, позволяя браузеру корректно перехватывать события клика.</li>
        <li><strong>Контроль пропорций:</strong> Изображение управляется связкой <code>relative</code> на контейнере и <code>object-cover</code> на компоненте <code>Image</code>. Это гарантирует заполнение области 96x96px без искажения геометрии фото.</li>
    </ul>

    <h3>2. На что обратить внимание в Tailwind CSS (Checklist)</h3>
    <ul>
        <li><strong>Состояния группы:</strong> Класс <code>group</code> на родительском элементе <code>article</code> позволяет синхронно управлять анимациями дочерних элементов (например, сменой цвета или появлением тени) при наведении на любую часть карточки.</li>
        <li><strong>Адаптивность тегов:</strong> Контейнер с <code>flex-wrap gap-2</code> предотвращает выход элементов за границы карточки, обеспечивая их мягкий перенос на новую строку при изменении длины текста.</li>
        <li><strong>Ограничение контента:</strong> Класс <code>line-clamp-3</code> в описании принудительно обрезает текст, сохраняя визуальное единообразие и одинаковую высоту карточек в одном ряду.</li>
    </ul>

    <h3>3. Что может НЕ работать (Критические нюансы)</h3>
    <ul>
        <li><strong>Вложенные ссылки:</strong> Согласно спецификации HTML5, вложение тега <code>&lt;a&gt;</code> в другой <code>&lt;a&gt;</code> запрещено. Если компонент <code>LabelLink</code> рендерит ссылку, возникнет конфликт приоритетов клика. В данной конфигурации рекомендуется использовать <code>span</code> для тегов.</li>
        <li><strong>Проброс классов:</strong> Утилитарные классы размера (например, <code>size-3</code>) не применятся к иконке, если компонент <code>RightIcon</code> не настроен на получение <code>className</code> и его передачу на корневой SVG-тег.</li>
        <li><strong>Конфликт Fill:</strong> Проп <code>fill</code> у компонента <code>Image</code> требует наличия <code>relative</code> у ближайшего родителя. В противном случае изображение растянется на всю высоту страницы.</li>
    </ul>

    <h3>📋 Шпаргалка по оптимизации</h3>
    <p>
    1. <strong>Z-index Management:</strong> При добавлении любых новых интерактивных элементов (иконки избранного, тултипы) им обязательно должен назначаться <code>z-10</code>, чтобы они не оказались под основной ссылкой.<br/>
    2. <strong>Image Fallback:</strong> Необходимо предусматривать безопасное значение для <code>src</code> (например, пустую строку или путь к заглушке), чтобы избежать фатальных ошибок Next.js при отсутствии данных.<br/>
    3. <strong>Производительность:</strong> Плавность визуальных состояний при наведении обеспечивается связкой <code>transition-all duration-200</code>.
    </p>
  `,
}

const vendorCardMock = {
  title: 'Vendor Card',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/cards/vendor-card/vendor-card.tsx',
  descriptionContent: `
    <h3>Анализ компонента VendorCard</h3>
    <p>
    Компонент представляет собой сложную карточку объекта с адаптивной сеткой, использующую паттерн "Full-Card-Click" и строгую визуальную иерархию.
    </p>

    <h3>1. Архитектура и Логика</h3>
    <ul>
        <li><strong>Паттерн «Обертка-ссылка»:</strong> Вся карточка является кликабельной благодаря абсолютно позиционированной ссылке <code>Link</code> (<code>absolute size-full</code>).</li>
        <li><strong>Управление слоями (Z-index):</strong> Чтобы кнопки оставались интерактивными поверх ссылки, им задан <code>relative z-10</code>. Это критический нюанс: без него клик по кнопке перехватит основная ссылка карточки.</li>
        <li><strong>Безопасность рендеринга:</strong> Использование логического И (<code>tags && ...</code>) предотвращает ошибки выполнения (runtime errors), если массив тегов не будет передан в пропсах.</li>
    </ul>
    <h3>2. Стилизация и Сетка (Responsive)</h3>
    <p>Карточка радикально меняет свою структуру на брейкпоинте <strong>xl (1280px)</strong>:</p>
    <table style="width: 100%; border-collapse: collapse;">
        <thead>
            <tr style="border-bottom: 1px solid #ccc;">
                <th style="text-align: left;">Элемент</th>
                <th style="text-align: left;">Mobile / Tablet (&lt; xl)</th>
                <th style="text-align: left;">Desktop (&ge; xl)</th>
            </tr>
        </thead>
        <tbody>
            <tr>
                <td><strong>Ориентация</strong></td>
                <td><code>flex-col</code> (вертикально)</td>
                <td><code>flex-row</code> (горизонтально)</td>
            </tr>
            <tr>
                <td><strong>Изображение</strong></td>
                <td>На всю ширину (<code>h-50</code>)</td>
                <td>Квадрат/Фикс (<code>w-60</code>)</td>
            </tr>
            <tr>
                <td><strong>Кнопки</strong></td>
                <td>В ряд (<code>justify-between</code>)</td>
                <td>В колонку (<code>flex-col</code>)</td>
            </tr>
        </tbody>
    </table>

    

    <h3>3. Критические точки (Где может "сломаться")</h3>
    <ul>
        <li><strong>Z-index конфликт:</strong> Любой новый интерактивный элемент внутри карточки обязан иметь <code>z-10</code>, иначе он будет "погребен" под ссылкой-подложкой.</li>
        <li><strong>SEO & Accessibility:</strong> Прозрачная ссылка <code>absolute size-full</code> требует добавления <code>aria-label</code> или скрытого текста внутри, чтобы скринридеры понимали, куда ведет эта ссылка.</li>
        <li><strong>Длинный контент:</strong> <code>line-clamp-5</code> спасает описание, но заголовок <code>Title</code> не имеет ограничений — экстремально длинный текст может некрасиво растянуть карточку по вертикали.</li>
    </ul>

    <h3>📋 Шпаргалка по коду</h3>
    <p>
    ✅ <strong>Главная фишка:</strong> Кликабельность всей площади + работающие кнопки внутри за счет Z-индексов.<br/>
    ✅ <strong>UX-оптимизация:</strong> Кнопка «Избранное» адаптивна — на мобильных устройствах текст скрывается (<code>hidden sm:inline</code>), оставляя только иконку для экономии пространства.<br/>
    ✅ <strong>Графика:</strong> Контейнер изображения с <code>overflow-hidden</code> и <code>object-cover</code> гарантирует, что логотипы поставщиков не будут деформироваться при изменении размеров.
    </p>
`,
}

const projectCardTextsMock = {
  title: 'Project Card',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/cards/project-card/project-card.tsx',
  descriptionContent: `
    <h3>Анализ компонента Project Card</h3>
    <p>
    Компонент реализует современную карточку с применением эффектов глубины (Glassmorphism) и строгим соблюдением пропорций макета, что характерно для высокоуровневых интерфейсов 2026 года.
    </p>

    <h3>1. Визуальные особенности и UX</h3>
    <ul>
        <li><strong>Слоистая прозрачность:</strong> Использование <code>backdrop-blur-md</code> и <code>bg-white/20</code> создает эффект матового стекла. Это добавляет интерфейсу глубины, позволяя фоновому изображению мягко просвечивать сквозь информационные плашки.</li>
        <li><strong>Двойной защитный градиент:</strong> Конструкция <code>bg-linear-to-t</code> (синтаксис Tailwind 5) с точками <code>from-black/40 via-transparent to-black/40</code> решает две задачи: делает читаемыми белые теги сверху и четко выделяет имя девелопера снизу.</li>
        <li><strong>Графическая стабильность:</strong> Класс <code>object-cover</code> в сочетании с жестким <code>aspect-129/101</code> гарантирует, что фотографии объектов не будут деформироваться (растягиваться), сохраняя естественные пропорции архитектуры.</li>
    </ul>

    <h3>2. Стилизация и Сетка</h3>
    <p>Технические параметры контейнера обеспечивают предсказуемое поведение карточки в составе сложных сеток:</p>
    <table style="width: 100%; border-collapse: collapse; margin-bottom: 16px;">
        <thead>
            <tr style="border-bottom: 2px solid #eee;">
                <th style="text-align: left; padding: 8px;">Свойство</th>
                <th style="text-align: left; padding: 8px;">Значение / Эффект</th>
            </tr>
        </thead>
        <tbody>
            <tr style="border-bottom: 1px solid #eee;">
                <td style="padding: 8px;"><strong>Распределение</strong></td>
                <td style="padding: 8px;"><code>basis-0 flex-1</code> — игнорирует размер контента для создания идеально равных колонок.</td>
            </tr>
            <tr style="border-bottom: 1px solid #eee;">
                <td style="padding: 8px;"><strong>Геометрия</strong></td>
                <td style="padding: 8px;"><code>h-[225.48px]</code> — прецизионная высота для точного соответствия Figma-макету.</td>
            </tr>
            <tr style="border-bottom: 1px solid #eee;">
                <td style="padding: 8px;"><strong>Иерархия</strong></td>
                <td style="padding: 8px;"><code>z-10</code> на лейблах — предотвращает визуальное "поглощение" текста градиентными слоями.</td>
            </tr>
        </tbody>
    </table>

    <h3>3. Критические точки (Узкие места)</h3>
    <ul>
        <li><strong>Переполнение контентом:</strong> Экстремально длинный заголовок <code>devTitle</code> может "распереть" карточку по вертикали, нарушая симметрию ряда. Рекомендуется ограничение <code>line-clamp-1</code>.</li>
        <li><strong>Избыток меток:</strong> При большом количестве <code>labels</code> и включенном <code>flex-wrap</code>, теги могут перекрыть центральную часть изображения или "выпасть" за его пределы. Оптимально использовать <code>.slice(0, 3)</code>.</li>
        <li><strong>Адаптивность высоты:</strong> Фиксированная высота <code>h-[225.48px]</code> при падении ширины вьюпорта ниже 240px может привести к избыточной обрезке значимых частей изображения.</li>
    </ul>

    <h3>📋 Рекомендации по коду</h3>
    <p>
    ✅ <strong>UX-совет:</strong> Добавьте свойство <code>min-h</code> для текстового блока под картинкой. Это выровняет карточки по нижней линии, даже если цена или название занимают разное количество строк.<br/>
    ✅ <strong>Безопасность:</strong> При итерации массива <code>labels</code> рекомендуется использовать уникальные ID вместо индексов массива для стабильной работы React Reconciliation.<br/>
    ✅ <strong>Доступность:</strong> Проверьте контрастность текста на плашках с <code>backdrop-blur</code> при использовании очень светлых изображений.
    </p>
`,
}

const textDisclosureAnimationProjectCardTextsMock = {
  title: 'Раскрывающейся Карточки',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/cards/text-disclosure-animation-project-card/text-disclosure-animation-project-card.tsx',
  descriptionContent: `
    <h3>Анализ компонента Раскрывающейся Карточки</h3>
      <p>
      Компонент реализует сложную анимацию раскрытия текстового контента без использования JavaScript-расчетов высоты, опираясь на современные возможности CSS Grid и JIT-двигателя Tailwind.
      </p>
      <h3>1. Магия анимации: grid-rows-[0fr] → [1fr]</h3>
      <ul>
          <li><strong>Механика:</strong> Это единственный стандартный способ плавно анимировать высоту от <code>0</code> до <code>auto</code>. Использование <code>grid-template-rows</code> позволяет браузеру вычислить конечную высоту дочернего элемента до начала анимации.</li>
          <li><strong>Критическое условие:</strong> Анимация сработает только при наличии <code>overflow: hidden</code> у внутреннего контейнера. Без него контент будет виден сразу, нарушая логику раскрытия.</li>
          <li><strong>Технический нюанс:</strong> Убедитесь, что ваш компилятор поддерживает произвольные значения (JIT). В Tailwind v3+ и v4 это работает из коробки, генерируя специфический CSS для этого перехода.</li>
      </ul>

      <h3>2. Синхронизация контента (UX/UI)</h3>
      <p>Использование двух разных полей (<code>description</code> и <code>description_long</code>) требует особого внимания к деталям:</p>
      <table style="width: 100%; border-collapse: collapse; margin-bottom: 16px;">
          <thead>
              <tr style="border-bottom: 2px solid #eee;">
                  <th style="text-align: left; padding: 8px;">Проблема</th>
                  <th style="text-align: left; padding: 8px;">Решение / Последствие</th>
              </tr>
          </thead>
          <tbody>
              <tr style="border-bottom: 1px solid #eee;">
                  <td style="padding: 8px;"><strong>Мерцание при переключении</strong></td>
                  <td style="padding: 8px;">Мгновенное исчезновение короткого текста при начале анимации создает визуальный "скачок". Рекомендуется использовать плавный <code>opacity</code>.</td>
              </tr>
              <tr style="border-bottom: 1px solid #eee;">
                  <td style="padding: 8px;"><strong>Логика чтения</strong></td>
                  <td style="padding: 8px;">Важно, чтобы <code>description_long</code> начинался идентично короткой версии, иначе пользователь теряет фокус при раскрытии.</td>
              </tr>
              <tr style="border-bottom: 1px solid #eee;">
                  <td style="padding: 8px;"><strong>Пустые данные</strong></td>
                  <td style="padding: 8px;">Если <code>description_long</code> отсутствует, кнопка развертывания должна быть скрыта (условие в коде).</td>
              </tr>
          </tbody>
      </table>

      <h3>3. Позиционирование и Производительность</h3>
      <ul>
          <li><strong>Риск Absolute-кнопки:</strong> Использование <code>absolute right-0 -bottom-6</code> опасно тем, что при малом объеме текста кнопка может перекрыть основные элементы управления (кнопку "Подробнее").</li>
          <li><strong>Оптимизация рендеринга:</strong> Избегайте <code>transition-all</code> на всей карточке. Замена на <code>transition-[transform,grid-template-rows]</code> существенно снижает нагрузку на графический процессор (GPU) мобильных устройств.</li>
          <li><strong>Геометрия:</strong> Фиксированная высота <code>h-[225.48px]</code> для изображения хороша для макета, но требует проверки на узких экранах (<code>min-w-60</code>), чтобы избежать искажения <code>aspect-ratio</code>.</li>
      </ul>

      <h3>📋 Финальный Чек-лист</h3>
      <p>
      ✅ <strong>Доступность:</strong> Обязательно добавьте <code>aria-expanded={isExpanded}</code> на кнопку переключения для корректной работы скринридеров.<br/>
      ✅ <strong>Конфигурация:</strong> Проверьте наличие переменных <code>bg-(--color-secondary)</code> в вашем CSS/Theme файле. Без них фон может не отобразиться.<br/>
      ✅ <strong>Анимация иконок:</strong> Сочетание инлайнового стиля <code>transform: rotate</code> и класса <code>transition-transform</code> обеспечит идеальное вращение стрелки <code>DownIcon</code> на 180°.<br/>
      ✅ <strong>Интерфейс:</strong> Ховер-эффекты на кнопках (<code>hover:brightness-110</code>) и тенях карточки создают завершенный вид премиального продукта.
      </p>
      `,
}

export {
  CardMock,
  ProjectCardMock,
  serviceCardMock,
  serviceSnippetCardMock,
  vendorCardMock,
  projectCardTextsMock,
  textDisclosureAnimationProjectCardTextsMock,
}
