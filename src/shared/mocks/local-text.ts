const fullscreenCarouselMock = {
  title: 'Одиночный слайдер с внутренней навигацией  (embla-carousel)',
  linkToCode:
    'https://github.com/alimbaeva/components/blob/main/src/widgets/slider/embla-carousel/fullscreen-carousel/fullscreen-carousel.tsx',
  descriptionContent: `
        <h3>Техническая архитектура и особенности слайдера</h3>
        <p>Компонент реализован с использованием <strong>Embla Carousel</strong> и оптимизирован под стандарты <strong>Next.js 16</strong>. Основные характеристики:</p>
        
        <ul>
            <li><strong>Конфигурация «Один слайд»:</strong> Свойство <code>flex-[0_0_100%]</code> гарантирует, что каждый слайд занимает ровно 100% ширины контейнера.</li>
            <li><strong>Бесшовная цикличность:</strong> Параметр <code>loop: true</code> обеспечивает бесконечную прокрутку в обе стороны.</li>
            <li><strong>Адаптивные пропорции:</strong> Использование <code>aspect-5/2</code> (или кастомных соотношений) позволяет контейнеру резервировать место на странице до загрузки изображений, предотвращая сдвиги верстки (CLS).</li>
        </ul>

        <h3>Критические моменты реализации (Critical points):</h3>
        <ul>
            <li><strong>Оптимизация LCP:</strong> Атрибут <code>priority={ind === 0}</code> заставляет браузер загружать первый слайд мгновенно, что критично для Core Web Vitals.</li>
            <li><strong>Flexbox хак <code>min-w-0</code>:</strong> Необходим для предотвращения «раздувания» слайдов под давлением внутреннего контента.</li>
            <li><strong>Умное заполнение <code>fill</code>:</strong> Использование Next.js Image с <code>object-cover</code> позволяет адаптировать картинку под любые пропорции без нарушения визуального соотношения сторон.</li>
            <li><strong>Интерактивность и навигация:</strong> Кнопки управления используют <code>backdrop-blur</code> и <code>transition</code> для мягкого визуального отклика, позиционированы абсолютно относительно <code>rounded-2xl</code> контейнера.</li>
        </ul>

        <h3>Безопасность и SEO:</h3>
        <ul>
            <li><strong>Доступность (A11y):</strong> Интерактивные элементы поддерживают стандартные атрибуты навигации, а структура семантически понятна поисковым роботам.</li>
        </ul>
    `,
}

const boundedSliderMock = {
  title: 'Ограниченный слайдер - Лента карточек (embla-carousel)',
  linkToCode:
    'https://github.com/alimbaeva/components/blob/main/src/widgets/slider/embla-carousel/bounded-slider/bounded-slider.tsx',
  descriptionContent: `
        <h3>Технические особенности и архитектура</h3>
        <p>
        Компонент представляет собой горизонтальную ленту (Carousel Row), оптимизированную для отображения группы карточек с инерционной прокруткой. В отличие от полноэкранных решений, данная реализация ориентирована на свободное взаимодействие и сохранение фиксированных пропорций элементов.
        </p>

        <h3>1. Конфигурация Embla Carousel</h3>
        <ul>
        <li>
            <strong>Инерционная навигация (<code>dragFree: true</code>):</strong> Параметр позволяет пользователю свободно прокручивать ленту. Контент не «примагничивается» жестко к границам слайдов, а плавно останавливается в зависимости от силы жеста, что соответствует нативному поведению списков в мобильных ОС.
        </li>
        <li>
            <strong>Оптимизация прокрутки (<code>containScroll: 'trimSnaps'</code>):</strong> Данная настройка исключает появление избыточного пустого пространства в конце ленты. Слайдер автоматически выравнивает крайние элементы по границам контейнера, предотвращая «перекрутку».
        </li>
        <li>
            <strong>Выравнивание (<code>align: 'start'</code>):</strong> Гарантирует, что при навигации карточка всегда центрируется по левому краю контейнера, обеспечивая предсказуемый визуальный ритм.
        </li>
        </ul>

        <h3>2. Верстка и визуальные стандарты</h3>
        <ul>
        <li>
            <strong>Система пропорций (<code>aspect-video</code>):</strong> Применение CSS-свойства <code>aspect-ratio</code> (16:9) на уровне карточки обеспечивает визуальное единообразие. Это избавляет от необходимости жестко фиксировать высоту в пикселях и гарантирует корректную адаптацию при изменении ширины слайда.
        </li>
        <li>
            <strong>Управление потоком (<code>shrink-0</code>, <code>min-w-0</code>):</strong> Данные свойства предотвращают деформацию карточек внутри Flex-контейнера. Элементы сохраняют заданную ширину (<code>w-80</code>), формируя корректную область прокрутки.
        </li>
        <li>
            <strong>Ограничение контейнера (<code>xl:max-w-200</code>):</strong> На сверхшироких экранах область слайдера ограничивается, что предотвращает чрезмерное растягивание контента и сохраняет композиционный баланс страницы.
        </li>
        </ul>

        <h3>3. Производительность и работа с медиа</h3>
        <ul>
        <li>
            <strong>Оптимизация загрузки (<code>sizes='320px'</code>):</strong> Атрибут указывает браузеру точный размер целевого изображения. Это позволяет Next.js генерировать оптимальные копии файлов, существенно снижая объем передаваемого трафика.
        </li>
        <li>
            <strong>Заполнение пространства (<code>fill</code>, <code>object-cover</code>):</strong> Комбинация данных параметров позволяет использовать изображения с разным исходным соотношением сторон. Изображение автоматически заполняет область карточки без искажения пропорций.
        </li>
        </ul>

        <h3>Критические нюансы (Important notes):</h3>
        <p>
        При использовании <code>overflow-hidden</code> на основном контейнере следует учитывать, что внешние эффекты (например, <code>box-shadow</code>) могут обрезаться. Для их корректного отображения рекомендуется использовать внутренние отступы (padding). Текущая реализация сфокусирована на сенсорном и drag-взаимодействии; для десктопных версий возможна интеграция дополнительных кнопок управления.
        </p>
    `,
}

const boundedControlledSliderMock = {
  title: 'Контролируемую реализацию Embla Carousel',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/slider/embla-carousel/bounded-controlled-slider/bounded-controlled-slider.tsx',
  descriptionContent: `
        <h3>Технические особенности и архитектура</h3>
        <p>
        Компонент <strong>BoundedControlledSlider</strong> представляет собой управляемую реализацию Embla Carousel. Это горизонтальная лента, оптимизированная для плавной навигации по группе карточек с использованием внешнего состояния (React State) для синхронизации элементов управления.
        </p>

        <h3>1. Конфигурация и логика Embla</h3>
        <ul>
        <li>
            <strong>Инерционная навигация (<code>dragFree: true</code>):</strong> Позволяет пользователю свободно «пролистывать» контент. Слайдер не фиксируется жестко на каждом шаге, а плавно замедляется, что создает естественный пользовательский опыт (Native-like feel).
        </li>
        <li>
            <strong>Умное ограничение (<code>containScroll: 'trimSnaps'</code>):</strong> Автоматически обрезает лишние пустые зоны в начале и в конце списка. Это гарантирует, что первый и последний слайды всегда будут прижаты к краям контейнера.
        </li>
        <li>
            <strong>Синхронизация состояния:</strong> Реализована через событие <code>select</code>. Состояния <code>canScrollPrev</code> и <code>canScrollNext</code> динамически блокируют или активируют кнопки управления, предотвращая пустые клики.
        </li>
        </ul>

        <h3>2. Верстка и адаптивность</h3>
        <ul>
        <li>
            <strong>Геометрия карточек (<code>aspect-video</code>):</strong> Использование фиксированного соотношения сторон 16:9 через Tailwind-класс гарантирует, что все слайды будут иметь одинаковую высоту независимо от загруженного изображения.
        </li>
        <li>
            <strong>Гибкость элементов (<code>shrink-0</code>):</strong> Запрещает Flex-контейнеру сжимать карточки. Каждый элемент сохраняет заданную ширину (<code>w-80</code>), формируя стабильную область прокрутки.
        </li>
        <li>
            <strong>Визуальный индикатор (Active Dot):</strong> Пагинация реализована с акцентом на активный элемент. Текущий индекс выделяется увеличенной шириной (<code>w-8</code>) и цветом, обеспечивая понятную обратную связь.
        </li>
        </ul>

        <h3>3. Работа с медиа (Next.js Image)</h3>
        <ul>
        <li>
            <strong>Контейнерное заполнение (<code>fill</code>):</strong> Изображения используют стратегию заполнения родителя. В сочетании с <code>object-cover</code> это позволяет корректно отображать фотографии любого формата без нарушения пропорций верстки.
        </li>
        <li>
            <strong>Оптимизация:</strong> Использование компонента Image обеспечивает автоматическую ленивую загрузку (lazy loading) и генерацию современных форматов (WebP/Avif) на лету.
        </li>
        </ul>

        <h3>Критические нюансы (Important notes):</h3>
        <p>
        Слайдер является <strong>клиентским компонентом</strong>, так как использует хуки <code>useEffect</code> и <code>useCallback</code>. При динамическом изменении количества слайдов API карусели автоматически вызывает <code>reInit</code>, что корректно пересчитывает доступность прокрутки. Рекомендуется следить за тем, чтобы <code>slidesMock</code> не был пустым во избежание рендеринга пустой навигации.
        </p>
    `,
}

const thumbnailsSliderMock = {
  title: 'Связанные слайдеры Embla Carousel',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/slider/embla-carousel/thumbnails-slider/thumbnails-slider.tsx',
  descriptionContent: `
        <h3>Архитектура синхронизированных слайдеров (Master-Slave)</h3>
        <p>
        Компонент реализует паттерн <strong>Master-Slave</strong>, где основной горизонтальный слайдер является ведущим, а вертикальная лента миниатюр — ведомым навигатором. Это обеспечивает мгновенную синхронизацию контента и превью.
        </p>

        <h3>1. Ключевая логика (Как это работает)</h3>
        <ul>
            <li><strong>Событийный триггер:</strong> Событие <code>onSelect</code> на главном слайдере принудительно вызывает метод <code>thumbApi.scrollTo(index)</code>, обеспечивая автоматическую докрутку ленты превью.</li>
            <li><strong>Вертикальная математика (<code>axis: 'y'</code>):</strong> Embla переключает расчеты с ширины на высоту. Вьюпорт начинает искать границы сверху и снизу.</li>
            <li><strong>Свободный Drag (<code>dragFree: true</code>):</strong> Позволяет быстро пролистывать миниатюры независимо от основного слайда, при этом система всегда готова «притянуть» ленту к активному элементу.</li>
        </ul>

        

        <h3>2. Критические условия (Почему может не работать)</h3>
        <ul>
            <li><strong>Схлопывание высоты:</strong> Вертикальный слайдер требует строго ограниченной высоты родителя (<code>thumbRef</code>). Если задать <code>height: auto</code>, область прокрутки исчезнет.</li>
            <li><strong>Сжатие слайдов (<code>shrink-0</code>):</strong> Flex-контейнер по умолчанию пытается втиснуть все элементы в доступную высоту. Без <code>shrink-0</code> миниатюры сожмутся в тонкие полоски, и скролл не активируется.</li>
            <li><strong>Конфликт <code>h-full</code>:</strong> Внутренний контейнер должен быть <code>h-auto</code>. Только когда его физический размер больше «окна» просмотра, создаются условия для работы Embla.</li>
        </ul>

        <h3>3. Best Practices и оптимизация</h3>
        <ul>
            <li><strong>Выравнивание (<code>align: 'start'</code>):</strong> В вертикальных превью это гарантирует предсказуемое появление активной картинки сверху, избегая лишних «скачков» ленты.</li>
            <li><strong>Touch Action:</strong> Для предотвращения залипания страницы на мобильных устройствах используется класс <code>touch-pan-y</code>.</li>
            <li><strong>Производительность:</strong> Для превью используется <code>sizes="128px"</code>, что предотвращает загрузку тяжелых исходников изображений.</li>
        </ul>

        <h3>Адаптивность и мобильная стратегия (Responsive Design)</h3>
            <p>
            Компонент динамически трансформируется в зависимости от ширины экрана, меняя не только верстку, но и логику работы JavaScript-движка Embla.
            </p>

            <h3>1. Трансформация интерфейса</h3>
            <ul>
                <li><strong>Смена ориентации:</strong> Использование связки <code>flex-col sm:flex-row</code> перестраивает макет из вертикального стека (мобильные) в классическую горизонтальную галерею (десктоп).</li>
                <li><strong>Управление порядком (Order):</strong> С помощью утилит <code>order-1</code> и <code>order-2</code> мы перемещаем блок превью под основной слайдер на мобильных устройствах, сохраняя семантически правильную структуру HTML.</li>
                <li><strong>Контекстное скрытие:</strong> Кнопки навигации скрыты на мобильных устройствах (<code>hidden sm:flex</code>), так как управление жестами (свайп) является приоритетным для сенсорных экранов.</li>
            </ul>

            

            <h3>2. Динамическая конфигурация Embla</h3>
            <ul>
                <li><strong>Breakpoints API:</strong> Использование параметра <code>breakpoints</code> внутри <code>useEmblaCarousel</code> позволяет на лету менять направление оси (<code>axis: 'x'</code> для мобильных и <code>axis: 'y'</code> для десктопа). Это критично для корректной обработки инерции свайпа.</li>
                <li><strong>Геометрия слайдов:</strong> На мобильных устройствах миниатюры получают фиксированные размеры (<code>h-16 w-24</code>), формируя горизонтальную ленту, в то время как на десктопе они возвращаются к квадратному формату с использованием <code>aspect-square</code>.</li>
            </ul>

            <h3>3. Оптимизация UX на малых экранах</h3>
            <p>
            Для мобильной версии приоритет отдается "бесконечному" свободному скроллу (<code>dragFree</code>). Это позволяет пользователю быстро пролистывать галерею пальцем, в то время как программная синхронизация через <code>scrollTo</code> продолжает удерживать фокус на выбранном элементе при взаимодействии с основным слайдером.
            </p>

            <h3>⚠️ На что обратить внимание при рефакторинге:</h3>
            <p>
            При переходе на мобильную версию убедитесь, что контейнер превью не ограничен по ширине (должен быть <code>w-full</code>), иначе лента миниатюр может "обрезаться" без возможности скролла. Также проверьте, что свойство <code>shrink-0</code> сохранено для горизонтального режима, иначе Flexbox попытается сжать все миниатюры в одну линию.
            </p>

        <h3>📋 Чек-лист для отладки (Troubleshooting)</h3>
        <p>
        1. Проверьте ограниченную высоту у <code>thumbRef</code> (например, <code>h-125</code>).<br/>
        2. Убедитесь, что у элементов превью есть класс <code>shrink-0</code>.<br/>
        3. Проверьте наличие <code>axis: 'y'</code> в конфиге Embla.<br/>
        4. Подписан ли <code>useEffect</code> на событие <code>select</code> главного API?<br/>
        5. Скрыты ли нативные скроллбары (<code>scrollbar-none</code>) во избежание конфликтов управления.
        </p>
    `,
}

const multipleItemsSliderMock = {
  title: 'Групповой слайдер в Embla Carousel',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/slider/embla-carousel/multiple-items-slider/multiple-items-slider.tsx',
  descriptionContent: `
        <h3>Архитектура группового слайдера (Multiple Items)</h3>
        <p>
        Компонент настроен для отображения нескольких карточек в одной видимой зоне. Основная задача — обеспечить четкое перелистывание контента "страницами" (группами), а не по одному элементу.
        </p>

        <h3>1. Ключевая логика (Как это работает)</h3>
        <ul>
            <li><strong>Групповой скролл (<code>slidesToScroll: 2</code>):</strong> Настройка API, которая привязывает кнопки навигации к количеству видимых элементов. Один клик перемещает ленту сразу на 2 позиции.</li>
            <li><strong>Геометрия вьюпорта:</strong> Количество карточек регулируется исключительно через CSS. Класс <code>min-w-[50%]</code> на слайде гарантирует, что в видимой зоне всегда будет ровно две карточки.</li>
            <li><strong>Метод компенсации Gap:</strong> Использование отрицательного margin (<code>-ml-4</code>) на контейнере и padding (<code>pl-4</code>) на слайдах позволяет избежать проблем с расчетом "точек привязки" (snaps), которые часто возникают при использовании обычного CSS gap.</li>
        </ul>

        

        <h3>2. Критические условия (Почему может не работать)</h3>
        <ul>
            <li><strong>Игнорирование <code>shrink-0</code>:</strong> Без этого класса Flexbox попытается уместить все слайды в ширину одного экрана, сжимая их. В результате скролл станет невозможен, так как контент не будет выходить за границы вьюпорта.</li>
            <li><strong>Конфликт <code>loop</code> и <code>canScroll</code>:</strong> При включении бесконечного цикла (<code>loop: true</code>) методы <code>canScrollPrev/Next</code> всегда возвращают <code>true</code>. В этом случае кнопки никогда не перейдут в состояние <code>disabled</code>.</li>
            <li><strong>Контейнер для Image:</strong> Использование <code>fill</code> в Next.js требует, чтобы родительский блок имел <code>position: relative</code>. Без этого изображения "разлетятся" по всему документу.</li>
        </ul>

        <h3>3. Адаптивность и мобильная стратегия</h3>
        <ul>
            <li><strong>Смена кратности:</strong> С помощью Breakpoints API в Embla можно менять <code>slidesToScroll</code> с 1 (на мобилках) на 2 (на десктопе) для лучшего UX.</li>
            <li><strong>Управление кнопками:</strong> На мобильных устройствах кнопки управления программно отключаются или скрываются (<code>hidden sm:flex</code>), уступая место нативному тач-скроллу.</li>
            <li><strong>Оптимизация LCP:</strong> Поскольку в видимой зоне одновременно находятся две карточки, параметр <code>priority</code> установлен для первых двух индексов (<code>ind < 2</code>).</li>
        </ul>

        <h3>4. Настройка отзывчивости (Responsive Steps)</h3>
        <ul>
            <li><strong>Синхронизация CSS и JS:</strong> Чтобы слайдер работал корректно, брейкпоинты в Tailwind (например, <code>sm:min-w-[50%]</code>) должны совпадать с брейкпоинтами в конфиге Embla (<code>breakpoints: { '(min-width: 640px)': { slidesToScroll: 2 } }</code>).</li>
            <li><strong>Адаптивный slidesToScroll:</strong> Важно менять количество прокручиваемых элементов. На мобильных устройствах (где видна 1 карточка) установите <code>slidesToScroll: 1</code>, иначе при клике на стрелку пользователь "пролетит" мимо контента.</li>
            <li><strong>Динамическая ширина карточек:</strong> Используйте стандартную сетку Tailwind:
                <ul>
                    <li><code>min-w-full</code> — 1 карточка (Mobile)</li>
                    <li><code>sm:min-w-[50%]</code> — 2 карточки (Tablet)</li>
                    <li><code>lg:min-w-[33.33%]</code> — 3 карточки (Desktop)</li>
                </ul>
            </li>
            <li><strong>Оптимизация изображений (sizes):</strong> Обязательно обновляйте пропс <code>sizes</code> у компонента Image. Например: <code>sizes="(max-width: 640px) 100vw, 50vw"</code>. Это укажет браузеру загружать менее тяжелые изображения, когда карточки становятся меньше.</li>
        </ul>

        <h3>⚠️ Почему это важно:</h3>
        <p>
        Если изменить ширину карточек в CSS, но оставить <code>slidesToScroll: 2</code> на мобильных устройствах, возникнет "эффект перепрыгивания": пользователь нажмет кнопку, и слайдер прокрутит две карточки, хотя видел он только одну. Это ломает пользовательский опыт.
        </p>

        <h3>📋 Особенности для шпаргалки (Cheatsheet)</h3>
        <p>
        1. <strong>Правило 50%:</strong> Хотите 2 карточки — ставьте <code>min-w-[50%]</code>. Хотите 3 — <code>min-w-[33.33%]</code>.<br/>
        2. <strong>Безопасный Gap:</strong> Всегда используйте комбинацию padding + negative margin для стабильности snaps.<br/>
        3. <strong>Синхронизация кнопок:</strong> Всегда обновляйте стейт <code>canScroll</code> через событие <code>select</code>, чтобы избежать кликов в "никуда".<br/>
        4. <strong>Overflow:</strong> Родительский контейнер <code>emblaRef</code> всегда должен иметь <code>overflow-hidden</code>.
        </p>
    `,
}

const infiniteAutoplaySliderMock = {
  title: 'Бесконечый плавный слайдер (эффект Marquee)  (embla-carousel)',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/slider/embla-carousel/infinite-autoplay-slider/infinite-autoplay-slider.tsx',
  descriptionContent: `
    <h3>Архитектура плавного бесконечного слайдера (Marquee Effect)</h3>
    <p>
    Этот компонент реализует эффект "бегущей строки". В отличие от классических каруселей, здесь отсутствует понятие "активного слайда" и дискретного перелистывания — лента движется непрерывно с константной скоростью.
    </p>

    <h3>1. Технические особенности реализации</h3>
    <ul>
        <li><strong>Плагин AutoScroll:</strong> Вместо стандартного Autoplay используется специализированный плагин <code>embla-carousel-auto-scroll</code>. Он работает через <code>requestAnimationFrame</code>, что обеспечивает идеально линейное движение без рывков, которые характерны для CSS-переходов.</li>
        <li><strong>Конфигурация Speed:</strong> Параметр <code>speed</code> (рекомендуемое значение 1) определяет количество пикселей смещения за один кадр. Это позволяет добиться медленного, "премиального" скольжения.</li>
        <li><strong>Свободный режим (dragFree):</strong> Опция <code>dragFree: true</code> критически важна. Она отключает "магнитное" притяжение к краям слайдов, позволяя ленте останавливаться и продолжать движение в любой точке, что создает ощущение физической инерции.</li>
    </ul>

    

    <h3>2. На что обратить внимание (Checklist)</h3>
    <ul>
        <li><strong>Размеры вьюпорта:</strong> Контейнеру с <code>ref={emblaRef}</code> обязательно нужен класс <code>overflow-hidden</code>. Без него слайды выстроятся в одну бесконечную линию и сломают верстку страницы.</li>
        <li><strong>Свойство shrink-0:</strong> У каждого слайда (<code>flex-item</code>) должен быть класс <code>shrink-0</code>. Без него Flexbox попытается сжать все карточки, чтобы они поместились в ширину экрана, и движение не активируется.</li>
        <li><strong>Адаптивность (min-w):</strong> Использование <code>min-w-[40%] sm:min-w-[25%]</code> гарантирует, что на мобильных устройствах пользователь увидит часть следующей карточки, что визуально намекает на возможность скролла.</li>
    </ul>

    <h3>3. В каких случаях слайдер может НЕ работать</h3>
    <ul>
        <li><strong>Недостаточно контента:</strong> Параметр <code>loop: true</code> требует, чтобы суммарная ширина всех слайдов значительно превышала ширину вьюпорта. Если у вас всего 2-3 карточки, Embla не сможет "зациклить" ленту, и она просто застынет. <em>Решение: дублируйте массив данных, если слайдов мало.</em></li>
        <li><strong>Иерархия DOM:</strong> Реф <code>emblaRef</code> должен быть строго на родительском элементе, внутри которого находится прямой потомок с <code>display: flex</code>. Любая лишняя обертка между ними разорвет связь с движком.</li>
        <li><strong>Конфликт плагинов:</strong> Если одновременно подключены <code>Autoplay</code> и <code>AutoScroll</code>, они будут конфликтовать за управление позицией контейнера. Используйте только один из них.</li>
    </ul>

    <h3>📋 Шпаргалка по оптимизации</h3>
    <p>
    1. <strong>StopOnMouseEnter:</strong> Всегда включайте эту опцию для UX. Пользователь должен иметь возможность "поймать" взглядом понравившуюся карточку, просто наведя на нее курсор.<br/>
    2. <strong>Priority Images:</strong> Для "бегущей строки" важно проставить <code>priority</code> первым 3-4 изображениям, так как они попадают в поле зрения мгновенно при загрузке.<br/>
    3. <strong>Sizes:</strong> Указывайте корректный <code>sizes</code> (например, <code>25vw</code>), чтобы браузер не качал полные 4К-изображения для маленьких карточек в ленте.
    </p>
    <p>
    Если ты заметишь, что при первой загрузке слайдер "прыгает" или дергается, убедись, что в объекте slidesMock как минимум в два раза больше элементов, чем помещается на экране. Для min-w-[25%] (4 карточки в ряд) в массиве должно быть не менее 8-10 слайдов.
    </p>
`,
}

const hoverGalleryMock = {
  title: 'Hover to Switch Gallery',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/slider/react-gallery/hover-gallery/hover-gallery.tsx',
  descriptionContent: `
        <h3>Галерея с переключением по наведению (Hover Switcher)</h3>
        <p>
        Этот паттерн обеспечивает быстрый просмотр нескольких изображений без кликов. Основная механика базируется на разделении области карточки на невидимые интерактивные зоны.
        </p>

        <h3>1. Ключевая логика (Core Logic)</h3>
        <ul>
            <li><strong>Невидимое сегментирование:</strong> Поверх изображений создается прозрачный Flex-контейнер. Свойство <code>flex-1</code> на дочерних элементах автоматически делит ширину карточки на равные «чувствительные зоны».</li>
            <li><strong>Триггеры событий:</strong> Каждая зона слушает событие <code>onMouseEnter</code>, которое мгновенно обновляет состояние <code>activeIndex</code>, подменяя видимое изображение.</li>
            <li><strong>Аппаратное ускорение:</strong> Использование <code>opacity</code> вместо <code>display: none</code> позволяет браузеру плавно переключать кадры без пересчета геометрии страницы (Layout Shift).</li>
        </ul>

        

        <h3>2. Технические нюансы и Z-Index</h3>
        <p>Для корректной работы важно соблюдать строгую иерархию слоев:</p>
        <ul>
            <li><strong>Нижний слой:</strong> Изображения (<code>Image</code>) с абсолютным позиционированием.</li>
            <li><strong>Средний слой (z-10):</strong> Сетка невидимых детекторов наведения.</li>
            <li><strong>Верхний слой (z-20):</strong> Индикаторы-полоски. <em>Важно:</em> если индикаторы перекрывают детекторы, добавьте им <code>pointer-events-none</code>.</li>
            <li><strong>Сброс состояния:</strong> Чтобы при уходе мыши возвращалась обложка, добавьте <code>onMouseLeave={() => setActiveIndex(0)}</code> на внешний контейнер.</li>
        </ul>

        <h3>3. Что может "сломаться" и ограничения</h3>
        <ul>
            <li><strong>Мобильные устройства:</strong> События <code>hover</code> на тачскринах работают некорректно. Индикаторы внизу (полоски) полезны для визуализации, но для полноценной мобильной версии рекомендуется внедрять свайп-механику (например, через Embla).</li>
            <li><strong>Мертвые зоны:</strong> Избегайте использования <code>gap</code> между детекторами наведения. Любой зазор создаст область, где мышь находится над карточкой, но переключение не срабатывает.</li>
            <li><strong>Количество фото:</strong> Оптимальное количество — от 3 до 6. При 10+ фото зоны становятся слишком узкими, вызывая "дребезжание" и слишком быструю смену картинок.</li>
        </ul>

        <h3>📋 Чек-лист для отладки (Troubleshooting)</h3>
        <p>
        1. <strong>Прозрачность:</strong> Если картинки мигают, проверьте наличие <code>object-cover</code> и <code>fill</code>.<br/>
        2. <strong>Кликабельность:</strong> Если карточка является ссылкой, убедитесь, что детекторы не блокируют переход (события должны всплывать).<br/>
        3. <strong>LCP (Скорость):</strong> Всегда устанавливайте <code>priority={ind === 0}</code> для первого изображения, чтобы карточка не выглядела пустой при загрузке.
        </p>

        
    `,
}

const EdgeToEdge = {
  title: 'Горизонтальный слайдер (Edge-to-Edge Logic)',
  linkToCode:
    'https://github.com/alimbaeva/components/tree/main/src/widgets/slider/embla-carousel/edge-to-edge/edge-to-edge.tsx',
  descriptionContent: `
    <h3>Горизонтальный слайдер (Edge-to-Edge Logic)</h3>
    <p>
    Этот компонент реализует популярный дизайнерский паттерн, где контент начинается внутри стандартной сетки (Container), но уходит "в бесконечность" за правый край экрана.
    </p>

    <h3>1. Ключевые особенности реализации</h3>
    <ul>
        <li><strong>Эффект «Разрыва рамки»:</strong> Благодаря расчету <code>margin-right: calc(50% - 50vw)</code>, контейнер слайдера игнорирует ограничения родительского <code>max-w-[1000px]</code> и прижимается к правому краю монитора.</li>
        <li><strong>Мягкое затухание:</strong> Справа наложен градиентный слой (Overlay) с <code>z-10</code>, который создает эффект "растворения" карточек. Использование <code>pointer-events-none</code> на этом слое критически важно, чтобы он не блокировал взаимодействие со слайдами.</li>
        <li><strong>Свободный скролл (Drag Free):</strong> Настройка <code>dragFree: true</code> позволяет пользователю "кидать" ленту с инерцией, что делает просмотр этапов или преимуществ более естественным.</li>
        <li><strong>Умная обрезка (Trim Snaps):</strong> Параметр <code>containScroll: 'trimSnaps'</code> гарантирует, что прокрутка закончится ровно на последней карточке, не оставляя пустой серой зоны в конце.</li>
    </ul>

    <h3>2. Технические нюансы (Checklist)</h3>
    <ul>
        <li><strong>Иерархия слоев:</strong> Контейнеру обязателен <code>relative</code>. Если у любого из родительских элементов выше по дереву стоит <code>overflow: hidden</code>, эффект выхода за край экрана будет заблокирован.</li>
        <li><strong>Свойство shrink-0:</strong> Каждая карточка (<code>AdvantageCard</code>) должна быть обернута в блок с <code>shrink-0</code>. Без этого Flexbox сожмет элементы под ширину экрана, и возможность скролла исчезнет.</li>
        <li><strong>Адаптивные отступы:</strong> Для мобильных версий предусмотрен <code>pr-4</code>, чтобы при докрутке до конца последняя карточка не прилипала к краю экрана "намертво".</li>
    </ul>

    <h3>3. В каких ситуациях может не работать</h3>
    <ul>
        <li><strong>Асимметричные сетки:</strong> Расчет <code>calc(50% - 50vw)</code> работает идеально только при центральном расположении родителя. В сложной боковой верстке это может вызвать появление горизонтальной полосы прокрутки у всего сайта.</li>
        <li><strong>Недостаточное количество карточек:</strong> Если ширина всех карточек меньше ширины экрана, градиент справа может визуально "обрезать" статичный контент.</li>
        <li><strong>Safari на iOS:</strong> В редких случаях <code>100vw</code> в мобильных браузерах вызывает дергание из-за появления/скрытия панели адреса. Рекомендуется тестировать финальный результат на реальных устройствах.</li>
    </ul>

    <p>
    <strong>Краткий итог:</strong> Это универсальная обертка (HorizontalCarousel), которая через <code>children</code> позволяет превратить любой список элементов в премиальный горизонтальный скролл с эффектом глубины.
    </p>
`,
}

const titleLinksMock = {
  gitHub: 'Смотреть код на GitHub',
}

export {
  fullscreenCarouselMock,
  boundedSliderMock,
  boundedControlledSliderMock,
  thumbnailsSliderMock,
  titleLinksMock,
  multipleItemsSliderMock,
  infiniteAutoplaySliderMock,
  hoverGalleryMock,
  EdgeToEdge,
}
